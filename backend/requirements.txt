# ─────────────────────────────────────────────
# ExamLens — Member 3 (Exam Parser) Dependencies
# Python 3.11+ required
# ─────────────────────────────────────────────

# PDF text extraction (text-layer PDFs)
pdfplumber==0.11.4

# PDF rendering → image (for OCR fallback)
# NOTE: Use 'pymupdf' (not 'PyMuPDF==1.24.x') on Python 3.13 + Windows.
# PyMuPDF 1.24.x tries to compile from source and fails without Visual Studio.
# pymupdf 1.26+ ships pre-built wheels — no compiler needed.
pymupdf>=1.25.0

# OCR engine wrapper
pytesseract==0.3.13

# Image processing (used by PyMuPDF + pytesseract)
Pillow==10.4.0

# Web API framework (Member 6 owns the main FastAPI app,
# but we expose our own route for independent testing)
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart>=0.0.9

# Data validation & models
pydantic==2.9.2

# Environment variables (.env support)
python-dotenv==1.0.1

# Testing
pytest==8.3.3
pytest-cov==5.0.0

# Utilities
httpx==0.27.2      # For testing FastAPI endpoints
